RewriteEngine On

# If the file or directory exists, serve it directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirect all requests to index.html (React app entry point)
RewriteRule ^ index.html [QSA,L]
